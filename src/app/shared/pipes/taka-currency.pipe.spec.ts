import { TakaCurrencyPipe } from './taka-currency.pipe';

describe('TakaCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new TakaCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
